import React from 'react';

const EngLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      This is layout in English ver.
      {children}
    </div>
  );
};

export default EngLayout;
